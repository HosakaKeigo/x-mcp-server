# X (Twitter) API Credentials
# Get these from https://developer.x.com/en/portal/dashboard
#
# NOTE: All environment variables are validated using t3-env at runtime.
# The server will fail to start if required variables are missing or invalid.
# See src/env.ts for the validation schema.

# API Key (Consumer Key) - REQUIRED
X_API_KEY=your_api_key_here

# API Secret (Consumer Secret) - REQUIRED
X_API_SECRET=your_api_secret_here

# Access Token - REQUIRED
X_ACCESS_TOKEN=your_access_token_here

# Access Token Secret - REQUIRED
X_ACCESS_TOKEN_SECRET=your_access_token_secret_here

# Bearer Token (Optional, for OAuth 2.0) - OPTIONAL
X_BEARER_TOKEN=your_bearer_token_here

# Node Environment (Optional, defaults to 'development')
# Valid values: development, production, test
NODE_ENV=development
